# pip install -r requirements.txt

# Base ----------------------------------------
# matplotlib>=3.2.2 install via sudo apt-get install python-matplotlib
# numpy>=1.18.5 install via sudo apt-get install python3-numpy
# opencv-python>=4.1.2 install via sudo apt-get install python3-opencv
# Pillow>=7.1.2 install via sudo apt-get install python3-pillow
PyYAML>=5.3.1
requests>=2.23.0
# scipy>=1.4.1 install via sudo apt-get install python3-scipy
torch>=1.7.0
torchvision>=0.8.1
tqdm>=4.41.0
serial

# Logging -------------------------------------
tensorboard>=2.4.1
thop  # FLOPs computation

# pyrealsense2 Install via setup_realsense script under scripts folder
